services:
  docs:
    image: mhai_django_local_docs
    container_name: mhai_django_local_docs
    build:
      context: .
      dockerfile: ./compose/local/docs/Dockerfile
    env_file:
      - ./.envs/.local/.django
    volumes:
      - ./docs:/docs:z
      - ./config:/app/config:z
      - ./mhai_django:/app/mhai_django:z
    ports:
      - '9000:9000'
    command: /start-docs
